<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.jjayyyyyyy.lit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HackerNewsActivity"
            android:label="@string/hacker_news"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".SolidotActivity"
            android:label="@string/solidot"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".NewsflashActivity"
            android:label="@string/newsflash"
            android:screenOrientation="portrait"/>
    </application>

</manifest>